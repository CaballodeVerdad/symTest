{% extends 'base.html.twig' %}

{% block title %}Examen{% endblock %}

{% block body %}
    <h1>Examen</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ examan.id }}</td>
            </tr>
            <tr>
                <th>Ano</th>
                <td>{{ examan.ano }}</td>
            </tr>
            <tr>
                <th>Periodo</th>
                <td>{{ examan.periodo }}</td>
            </tr>
            <tr>
                <th>Mes</th>
                <td>{{ examan.mes }}</td>
            </tr>
            <tr>
                <th>Nombre_legible</th>
                <td>{{ examan.nombreLegible }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_examen_index') }}">back to list</a>

    <a href="{{ path('app_examen_edit', {'id': examan.id}) }}">edit</a>

    {{ include('examen/_delete_form.html.twig') }}
{% endblock %}
